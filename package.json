{
  "name": "encom",
  "version": "0.0.1",
  "description": "entity-component-system",
  "scripts": {
    "dev": "nodemon --exec babel-node source/main.js --source-maps inline",
    "build": "babel source --out-dir build",
    "start": "node build/main.js"
  },
  "dependencies": {
    "event-emitter": "^0.3.4"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "nodemon": "^1.9.2"
  }
}
